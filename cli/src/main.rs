//! CLI (Phase 6): start + target + tolerance in, ranked Lineup[] JSON out.
//! Phase 0 scaffold; the real interface lands in Phase 6.

fn main() {
    eprintln!("recon-cli: not implemented yet (Phase 6). See plan.md.");
    std::process::exit(2);
}
